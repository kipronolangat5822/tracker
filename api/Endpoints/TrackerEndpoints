using System.ComponentModel;

namespace api.Endpoints
{
public static class TrackerEndpoints
    {
        public static void MapTrackerEndpoints(this IEndpointRouteBuilder routes)
        {
            var group = routes.MapGroup("/api/trackers");
        }
    }
}